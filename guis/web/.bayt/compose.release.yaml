services:
  guis_web-release:
    build:
      additional_contexts:
        guis_web-build: service:guis_web-build
      context: ..
      dockerfile: .bayt/Dockerfile.release
      target: release
    image: bayt-guis_web-release:latest
