# generated from bayt.cue — do not edit
tasks:
  default:
    deps:
    - ::bayt:setup
    status:
    - ../../plugins/bayt/runtime/bayt fingerprint --manifest {{.TASKFILE_DIR}}/bayt.build.json --stamp-file .task/bayt/build.hash
    vars:
      BAYTW: ../../plugins/bayt/runtime/bayt cache run --manifest {{.TASKFILE_DIR}}/bayt.build.json --full -- mise x --
    cmds:
    - cmd: '{{.BAYTW}} pwsh -c ".\\gradlew.bat --init-script .bayt/init.gradle.kts assemble"'
      platforms:
      - windows
    - cmd: '{{.BAYTW}} ./gradlew --init-script .bayt/init.gradle.kts assemble'
      platforms:
      - linux
    - cmd: '{{.BAYTW}} ./gradlew --init-script .bayt/init.gradle.kts assemble'
      platforms:
      - darwin
    - defer: '{{if not .EXIT_CODE}}../../plugins/bayt/runtime/bayt fingerprint --manifest {{.TASKFILE_DIR}}/bayt.build.json --stamp-file .task/bayt/build.hash --update-stamp{{end}}'
version: '3'
