# CLAUDE.md

Agent context for `libraries/mecha`. Everything here is stated once, elsewhere:

- **What makes a system a mecha** — the nine invariants — is `README.md`.
- **How v1, v2 and v3 satisfy them**, the components, the delivery guarantees
  and the cloud mappings, is `DESIGN.md`.
- **Running the stack, generating from schema, key files, and troubleshooting a
  broken pipeline** is `CONTRIBUTING.md`.
- **The arguments behind individual decisions** — schema evolution, the access
  layer, the connection ceiling — are dated docs under `docs/`.

Read `CONTRIBUTING.md` first when the task is operational, `DESIGN.md` when it
turns on why a component is what it is.
