# generated from bayt.cue — do not edit
tasks:
  default:
    run: always
    status:
    - bayt fingerprint --manifest {{.TASKFILE_DIR}}/bayt.doctor.json --stamp-file .task/bayt/doctor.hash
    vars:
      BAYTW: bayt cache run --manifest {{.TASKFILE_DIR}}/bayt.doctor.json --
    cmds:
    - '{{.BAYTW}} mise doctor'
    - defer: '{{if not .EXIT_CODE}}bayt fingerprint --manifest {{.TASKFILE_DIR}}/bayt.doctor.json --stamp-file .task/bayt/doctor.hash --update-stamp{{end}}'
version: '3'
