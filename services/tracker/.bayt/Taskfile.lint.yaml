# generated from bayt.cue — do not edit
tasks:
  default:
    run: always
    status:
    - bayt fingerprint --manifest {{.TASKFILE_DIR}}/bayt.lint.json --stamp-file .task/bayt/lint.hash
    vars:
      BAYTW: bayt cache run --manifest {{.TASKFILE_DIR}}/bayt.lint.json -- mise x --
    cmds:
    - '{{.BAYTW}} nu sayt.nu lint'
    - defer: '{{if not .EXIT_CODE}}bayt fingerprint --manifest {{.TASKFILE_DIR}}/bayt.lint.json --stamp-file .task/bayt/lint.hash --update-stamp{{end}}'
version: '3'
