# generated from bayt.cue — do not edit
services:
  services_tracker-integrate:
    build:
      additional_contexts:
        services_tracker-build: service:services_tracker-build
      context: ..
      dockerfile: .bayt/Dockerfile.integrate
      target: integrate
      x-bake:
        cache-from:
        - type=registry,ref=registry.depot.dev/f5k5087x1b:tracker-bake-cache-v1-${CACHE_SCOPE:-branch-bkversion-os-arch}-services_tracker-integrate
        - type=registry,ref=registry.depot.dev/f5k5087x1b:tracker-bake-cache-v1-${CACHE_SCOPE_FALLBACK:-branch-bkversion-os-arch}-services_tracker-integrate
        cache-to:
        - type=registry,ref=registry.depot.dev/f5k5087x1b:tracker-bake-cache-v1-${CACHE_SCOPE:-branch-bkversion-os-arch}-services_tracker-integrate,mode=min,image-manifest=true,oci-mediatypes=true
        output:
        - type=docker
    command:
    - mise
    - x
    - --
    - ./gradlew
    - --init-script
    - .bayt/init.gradle.kts
    - integrationTest
    - --rerun
    environment:
      TESTCONTAINERS_HOST_OVERRIDE: ${TESTCONTAINERS_HOST_OVERRIDE}
    image: bayt-services_tracker-integrate:latest
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock
