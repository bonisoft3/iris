# generated from bayt.cue — do not edit
FROM services_tracker-tx-build AS launch
WORKDIR /monorepo/services/tracker-tx
RUN --mount=type=cache,target=/zypper-store,id=/zypper-store,sharing=shared --mount=type=cache,target=/var/cache/zypp,id=services_tracker-tx-launch:/var/cache/zypp,sharing=locked s=/zypper-store; d=/var/cache/zypp/packages; mkdir -p "$s" "$d"; cp -r "$s"/. "$d"/ 2>/dev/null || true; zypper -n modifyservice --no-refresh --all && sed -i 's|http://|https://|g' /etc/zypp/repos.d/*.repo || exit; zypper -n modifyrepo --keep-packages --all && zypper -n install gettext-runtime=0.22.5-160000.2.2 || exit; cd "$d" && for f in */*.rpm */*/*.rpm; do [ -f "$f" ] || continue; o="$s/$f"; [ -e "$o" ] && continue; mkdir -p "$(dirname "$o")"; t="$(mktemp "$(dirname "$o")/.pXXXXXX")" && cp "$f" "$t" && mv -f "$t" "$o"; done; zypper -n modifyrepo --no-keep-packages --all || true
COPY --from=envoyproxy/envoy:v1.35.3@sha256:4d496918618a7ebd6c71ae8285e31ebff092f3a0a5ad642d50decf4a54eb2456 /usr/local/bin/envoy /usr/local/bin/envoy
ENV PATH=/root/.local/share/mise/shims:$PATH
COPY --from=libraries_xproto-descriptor_outs --parents /monorepo/libraries/xproto/out/xproto.desc.pb /
ENV GRPC_ADDRESS=127.0.0.1
ENV GRPC_PORT=50051
ENV GRPC_TRACKER_SERVICE=trash.tracker.v1.TrackerService
ENV GRPC_NEWS_SERVICE=trash.tracker.v1.NewsService
ENV GRPC_OBJECT_MATERIAL_SERVICE=trash.tracker.v1.ObjectMaterialService
ENV GRPC_DISPOSAL_PLACE_SERVICE=trash.tracker.v1.DisposalPlaceService
ENV PROTO_DESCRIPTOR_PB=/monorepo/libraries/xproto/out/xproto.desc.pb
ENV ENVOY_UID=0
RUN ["mise", "x", "--", "true"]
COPY .bayt/Taskfile.yml .bayt/Taskfile.bayt.yml .bayt/bayt.launch.json ./.bayt/
EXPOSE 8080

FROM busybox:musl@sha256:03db190ed4c1ceb1c55d179a0940e2d71d42130636a780272629735893292223 AS launch_ctxs
WORKDIR /monorepo/services/tracker-tx
COPY --from=services_tracker-tx-build_srcs /monorepo /monorepo
ARG SOURCE_DATE_EPOCH
ARG BAYT_CLAMP=1
RUN if [ "${BAYT_CLAMP:-1}" != 0 ]; then find /monorepo/services/tracker-tx -exec touch -hd @${SOURCE_DATE_EPOCH:-0} {} + && touch -hd @${SOURCE_DATE_EPOCH:-0} /monorepo /monorepo/services; fi
FROM scratch AS launch_srcs
COPY --from=launch_ctxs /monorepo /monorepo

FROM busybox:musl@sha256:03db190ed4c1ceb1c55d179a0940e2d71d42130636a780272629735893292223 AS launch_bayt_ctxs
WORKDIR /monorepo/services/tracker-tx
COPY --parents .bayt/compose.launch.yaml .bayt/Dockerfile.launch .bayt/bayt.launch.json .bayt/Taskfile.yml .bayt/Taskfile.bayt.yml .bayt/compose.launch.closure.yaml ./
COPY --from=services_tracker-tx-build_bayt /monorepo /monorepo
COPY --from=libraries_xproto-descriptor_bayt /monorepo /monorepo
COPY --from=services_tracker-tx-setup_bayt /monorepo /monorepo
ARG SOURCE_DATE_EPOCH
ARG BAYT_CLAMP=1
RUN if [ "${BAYT_CLAMP:-1}" != 0 ]; then find /monorepo/services/tracker-tx -exec touch -hd @${SOURCE_DATE_EPOCH:-0} {} + && touch -hd @${SOURCE_DATE_EPOCH:-0} /monorepo /monorepo/services; fi
FROM scratch AS launch_bayt
COPY --from=launch_bayt_ctxs /monorepo /monorepo
