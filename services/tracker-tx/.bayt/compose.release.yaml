# generated from bayt.cue — do not edit
services:
  services_tracker-tx-release:
    build:
      additional_contexts:
        services_tracker-tx-build_outs: service:services_tracker-tx-build_outs
      context: ..
      dockerfile: .bayt/Dockerfile.release
      target: release
      x-bake:
        platforms:
        - linux/amd64
    image: bayt-services_tracker-tx-release:${BAYT_IMAGE_TAG:-latest}
  services_tracker-tx-release_srcs:
    build:
      additional_contexts:
        services_tracker-tx-build_srcs: service:services_tracker-tx-build_srcs
      context: ..
      dockerfile: .bayt/Dockerfile.release
      target: release_srcs
    image: bayt-services_tracker-tx-release_srcs:latest
