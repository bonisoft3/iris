#!/bin/sh
# Get the directory where this script lives
DIR="$(dirname "$(readlink -f "$0")")" 

# Check for Alpine and prefer musl variant if available
if [ -f /etc/alpine-release ] && [ -f "$DIR/nu_plugin_gstat.musl.toml" ]; then
    "$DIR/.lazybox" trust -y -a -q .
    MISE_LOCKED=0 exec "$DIR/.lazybox" tool-stub "$DIR/nu_plugin_gstat.musl.toml" "$@"
else
    "$DIR/.lazybox" trust -y -a -q .
    MISE_LOCKED=0 exec "$DIR/.lazybox" tool-stub "$DIR/nu_plugin_gstat.toml" "$@"
fi
