tasks:
  default:
    run: always
    status:
    - bayt-runtime fingerprint --manifest {{.TASKFILE_DIR}}/bayt.doctor.json --stamp-file .task/bayt/doctor.hash
    vars:
      BAYTW: bayt-runtime cache run --manifest {{.TASKFILE_DIR}}/bayt.doctor.json --
    cmds:
    - '{{.BAYTW}} mise doctor'
    - defer: '{{if not .EXIT_CODE}}bayt-runtime fingerprint --manifest {{.TASKFILE_DIR}}/bayt.doctor.json --stamp-file .task/bayt/doctor.hash --update-stamp{{end}}'
version: '3'
